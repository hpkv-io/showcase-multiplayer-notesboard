# Ensure optional dependencies are included (needed for platform-specific binaries)
include=optional
# Install binaries for all platforms to ensure Linux binaries are available for Cloudflare deployment
target_platform=linux,win32,darwin
target_arch=x64,arm64 